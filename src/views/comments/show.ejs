
<% if(comment) { %>
  <div>
    <hr>
    <p>
      <%= comment.body %>
    </p>
    <small class="card-subtitle mb-2 text-muted">
      Sent by <%= comment.User.email %> on <%= comment.createdAt.toDateString() %>
    </small>
    <% if (currentUser) { %>
      <% if(currentUser && currentUser.id === comment.userId || currentUser.role === 'admin') { %>
        <form style="display:initial"
              action="/topics/<%= post.topicId %>/posts/<%= post.id %>/comments/<%= comment.id %>/destroy"
              method="post">
          <button type="submit" class="btn btn btn-sm btn-outline-danger">Delete</button>
        </form>
      <% } %>
    <% } %>
  </div>
<% } %>

