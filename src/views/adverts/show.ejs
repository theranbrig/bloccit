<!DOCTYPE html>
<html>

<head>
  <% include ../partials/head.ejs %>
</head>

<body>
  <% include ../partials/navbar.ejs %>
    <main class='container'>
      <a class='btn btn-primary' href='/adverts'>Back</a>
      <h1>
        <%= advert.title %>
      </h1>
      <p>
        <%= advert.description %>
      </p>
      <a href="/adverts/<%= advert.id %>/edit" class="btn btn-warning">Edit</a>
      <form style="display:initial" action="/adverts/<%= advert.id %>/destroy" method="post">
        <button type="submit" class="btn btn-danger">Delete</button>
      </form>
    </main>

    <% include ../partials/baseScripts.ejs %>

</body>

</html>
